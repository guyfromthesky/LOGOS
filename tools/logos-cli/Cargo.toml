[package]
name = "logos-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
tokio = { version = "1.0", features = ["full"] }
logos-core = { path = "../../crates/logos-core" }
logos-runtime = { path = "../../crates/logos-runtime" }
